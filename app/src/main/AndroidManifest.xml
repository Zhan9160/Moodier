<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laurier.joelucy.CP670project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/smile1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/smile1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GoalDetail"></activity>
        <activity
            android:name=".ToolBarActivity"
            android:label="@string/Login"
            android:layout="@layout/activity_tool_bar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListMoodRecord" />
        <activity android:name=".WriteMood" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogMoodActivity" />
        <activity android:name=".GoalActivity" />
        <activity android:name=".CreateGoalActivity" />
        <activity android:name=".SetGoalActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>